#include <iostream>
using namespace std;

int main(){
	int a, b, r, m;
	cout << "Dammi due numeri e ti diro'il loro massimo comune divisore" << endl;
	cout << "Il primo: ";
	cin >> a;
	cout << "Il secondo: ";
	cin >> b;
	if (b=0)
		cout << "Ecco a te: " << a;
	else
		r = a%b
	for (r = a%b;r!=0;){
		a + b;
		b 
		r = a%b;
	return 0;
}